# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
#chnageggit
#Action runners are linux so have to use linux commands
name: Deploy to Firebase Hosting on PR
on:
  push:
    branches:
      - hosting
  pull_request:
    branches:
      - master
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:

      #clear auth cache
      - name: Clear auth credentials
        run: rm -rf $HOME/.cache/google-auth-library

      # set credentials
      - name: set credentials
        run: echo "GOOGLE_APPLICATION_CREDENTIALS=$( cat "C:\Users\nicole\Downloads\rightstartmontessori-ebce0-firebase-adminsdk-qhvwe-3fa6de41a7.json")"

      # download copy of repository
      - name: Checkout code
        uses: actions/checkout@v3
      # update firebase tools
      - name: Install latest firebase-tools
        run: npm install -g firebase-tools

      - name: Deploy to firebase hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        # run these steps if no errors
        if: success()
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_RIGHTSTARTMONTESSORI_EBCE0 }}'
          projectId: rightstartmontessori-ebce0

      - name: Update firebase-tools version
        run: npm update firebase-tools
        if: failure()

      
